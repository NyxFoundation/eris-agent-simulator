# Non-secret Grafana settings (committed). Secrets (admin password, Slack token) live in
# grafana/secret.env, which is gitignored and created on the host.
GF_USERS_ALLOW_SIGN_UP=false
GF_USERS_DEFAULT_THEME=dark
