# config/example.yaml
#
# シミュレーションのユーザー設定の単一ソース。`config/local.yaml` にコピーして使う:
#   cp config/example.yaml config/local.yaml
#   npm run sim:realtime                              # 既定で config/local.yaml を読む
#   npm run sim:realtime -- --config config/stress.yaml   # 別ファイルを指定
#
# キーはネスト（run / funding / limits / flow / stress + agents）。値は型付きで書ける（真偽値・数値・
# 配列・オブジェクト）。未指定キーは既定値。一回限りの上書きは CLI フラグ（--seed / --blocks / --protocols 等）。
#
# 秘密情報はここに書かない（このファイルはコミットされうる）。RPC URL・秘密鍵・API キーは
# `.env.local` に置く（ARB_RPC_URL / *_PRIVATE_KEY / ANTHROPIC_API_KEY / OLLAMA_API_KEY）。
# ローカル Anvil の dev キーは未指定でも既定で使われる。

run:
  seed: 1
  blocks: 24
  seconds: 70
  blockTimeSec: 2
  protocols: [uniswap, balancer, curve]
  economicGas: false
  localDeploy: false
  reportDir: ./runs

funding:
  wethWei: "0" # USDC-only 配布（初期の方向性エクスポージャを排除）
  usdcUnits: "25000000000"
  # base: { WBTC: "0" }   # 追加 base の初期在庫（既定 0）

limits:
  agentWethWei: "1000000000000000000"
  agentUsdcUnits: "5000000000"
  # agentBase: { WBTC: "0" }   # 追加 base の per-round 上限

flow:
  uninformedMaxWethWei: "1000000000000000000"
  informedMaxWethWei: "2000000000000000000"
  balancerMaxWethWei: "1000000000000000000"
  curveMaxWethWei: "1000000000000000000"
  # 現実的フロー（ADR 0015 Notes / amm-challenge 由来）。以下は既定値で、書かなくても効く。
  #   informedArbFeeBps: 環境の裁定(informed flow)を fee-aware に。gap が fee バンド(既定 30bps)を
  #     超えたときだけ超過分を閉じ、残差=fee を残す（毎ブロック全力で fair を締めない）。0 で従来挙動。
  #   uninformedArrivalRate: 無情報フローを Poisson 到着に（λ、既定 0.9）。0 で固定本数に戻す。
  #   uninformedSizeSigma: 無情報フローのサイズを lognormal 化（σ、既定 1.0。裾が重い＝時々大口）。
  # 注: Poisson/lognormal は分散を増やすので run 比較は複数 seed の集計で読むこと。無効化は 0 に。
  informedArbFeeBps: 30
  uninformedArrivalRate: "0.9"
  uninformedSizeSigma: "1.0"
  # 同じ現実的フローを GMX / Aave にも展開（既定で有効。ADR 0015 Notes / amm-challenge）。
  #   gmxArrivalRate: GMX 建玉本数を Poisson(λ)、サイズを lognormal に（既定 0.75）。0 で従来の
  #     Bernoulli(gmxActivityProb)+一様バースト+一様サイズに戻す。gmxSizeSigma は lognormal の σ（既定 1.0）。
  #   aaveActorSizeSigma: Aave 各アクターの目標担保を lognormal で不均質化（whale/minnow の混在。既定 1.0）。
  #     borrow は各自の担保の 30% LTV に追従するので HF 安全は不変。0 で全アクター一律。
  gmxArrivalRate: "0.75"
  gmxSizeSigma: "1.0"
  aaveActorSizeSigma: "1.0"
  # gmx flow を出すブロック確率（gmxArrivalRate=0 の従来モードでのみ使用。0..1、既定 0.5）。
  # gmxActivityProb: "0.5"
  # gmxMaxBurst: "2"        # 従来モードで発火ブロックが出す gmx 本数の上限（1=常に単発）
  # aave 借り手プール: aaveActorCount 個の独立アクター（別アドレス・持続ポジション）。各アクターは
  # 毎ブロック aaveActivityProb の確率で借入/返済/担保/引出を 1 つ行い、借りた債務は翌ブロックも残る。
  # 1 ブロックの同時 borrow 数は最大 aaveActorCount（別アクターから自然発生）。
  # aaveActivityProb: "0.5"
  # aaveActorCount: "4"
  # マルチアセット(WBTC)を取引させるなら WBTC flow を有効化（価格乖離=裁定機会を作る）:
  # baseMax: { WBTC: "50000000" }

# stress（市場ストレスイベント。既定 off）:
# stress:
#   events:
#     - { type: crash, magnitudeRange: [0.12, 0.16], windowFrac: [0.3, 0.7], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
#   victimCount: 0

# agents（ロスター。旧 agents.*.json の置き換え。inline で書く）
# 注: account0 は deployer と重なるため、ローカルでは AGENT1 以降を使う。各 agent の env は
# その agent プロセスへ渡す戦略パラメータ（sim 設定とは別物）。
agents:
  - id: noop
    wallet: AGENT1_PRIVATE_KEY
    baseline: true
    description: 何もしない (基準ライン)
  - id: venue-arb
    wallet: AGENT2_PRIVATE_KEY
    description: WETH 専用 cross-venue 裁定
  - id: multi-arb
    wallet: AGENT3_PRIVATE_KEY
    description: base 非依存 cross-venue 裁定（全 active base × 全 venue）
