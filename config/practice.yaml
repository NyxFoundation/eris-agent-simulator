# The practice devnet (ADR 0021).
#
#   npm run sim:realtime -- --config config/practice.yaml
#
# A period, not a run: the chain does not stop, participants run their own agents against it, and
# the standings are practice standings — the competition itself is scored separately, from submitted
# bundles replayed over a scenario matrix (ADR 0017 / ADR 0020).
#
# Rules §2.7 promise "a configuration equivalent to the competition environment", so the venues, the
# endowment and the flow below are the official regimes' (config/regimes/calm.yaml), and the events
# are the official regimes' episodes, every kind every week. What cannot be equivalent is the reset:
# this is one world for the whole period, so the standings rank each day's *return* rather than its
# USDC (core/src/scoring/practiceReturn.ts has why), with every day weighted the same.
#
# The seed is not the one below. Every draw in the period -- the price walk, the flow, every event
# window -- is a pure function of it, and this file is public: with `seed: 1` anyone can compute the
# block each crash lands on. The hosted period passes `--seed` from a file that is not committed
# (infra/devnet/ascon-devnet.service). `seed: 1` is what a local run of this file gets.
#
# Two things have to be true of the chain this points at, and neither is set here:
#
#   ANVIL_RPC_URL / CHAIN_ID / TREASURY_PRIVATE_KEY in .env.local. They belong to a deployment, not
#   to a config a repo commits — this file must not name one operator's node.
#
#   run.chainMode: external once that chain is a real client (issue #35's OP Stack devnet). Left at
#   the default here so the file runs against a local anvil today; flipping it turns every cheatcode
#   into a refusal with the work item that replaces it named (issue #33).
#
# Hand participants their credentials one at a time:
#   npm run manifest -- --config config/practice.yaml                    # the public manifest
#   npm run manifest -- --config config/practice.yaml --participant <id> # their address (+ key)

run:
  seed: 1
  # The period ends on a date, not a block count (issue #136). Rules §2.7 end the trial on 10/31 and
  # the live week starts 11/1. Stated as `blocks: 1814400` (42 days) it ended 42 days after the
  # coordinator happened to start -- a start on 9/23 ran to ~11/4, into the live week -- and every
  # restart (a restart is a new competition) got a fresh 42 days. The coordinator converts this date
  # into the blocks that remain at blockTimeSec when it starts, so a restart ends on the same day.
  # It still needs a length rather than `blocks: 0`: an episode's window is placed as a fraction of
  # the run's length (ADR 0009). For a short smoke run of this file, `--blocks N` replaces the date.
  endsAt: "2026-10-31T23:59:59+09:00"
  # A ceiling, not the stop condition — a run with episodes ends on the block count so the time
  # limit cannot cut in before a window opens (ADR 0009 §4). Generous (42 days), so a chain running
  # behind cadence is not truncated.
  seconds: 3628800
  blockTimeSec: 2
  # 規約 §2.6 の確定値（docs/18 §12）。指定しないとコーディネータが既定の 30M でチェーンを
  # 上書きするので、anvil 側で 320M にしてあっても効かない。試行と本番でガス上限が違うと、
  # 参加者は本番に無いガス制約に合わせて調整することになる。
  blockGasLimit: 320000000
  protocols: [uniswap, balancer, curve, gmx, aave, lst, liquity]
  localDeploy: true
  # One world from start to finish, which is what a practice period is. The official competition
  # runs `scenario` (ADR 0020 §2), and only the matrix runner can declare it.
  resetUnit: continuous

  # A round stated in real time (ADR 0021 §3). Half an hour is the short end of the band the ADR
  # settles on: standings move roughly 48 times a day, which is often enough to watch and rare
  # enough that a round is a position rather than a tick. The block count follows from
  # blockTimeSec — an operator who changes the cadence should not silently change the round.
  #
  # The value here and the lambda that goes with it are the piece ADR 0021 leaves open (#56).
  epochSeconds: 1800

  # One directory per day (ADR 0021 §6). The chain stays continuous; only the artifacts are cut,
  # because a week of events.jsonl is a file nobody opens.
  segmentHours: 24
  segmentName: practice
  reportDir: ./runs

  # Registrations that arrive while the period is running (ADR 0021 §2, rules §2.7). The roster
  # below is read once, at startup; this file is re-read every ~30 blocks, and each new entry is
  # funded, attributed and scored from the next round on -- without a restart, which would open a new
  # competition directory and split the standings. Same entry shape as an `external: true` + `address`
  # roster entry; see config/registrations.example.yaml. Entries already in the roster are a no-op; a
  # duplicate id or address is ignored with a `registration_ignored` event; a malformed file is
  # reported once per edit (`registrations_reload_failed`) and never stops the run.
  registrationsFile: config/registrations.yaml

# The official regimes' endowment and flow, copied rather than referenced so a change there is a
# decision here too. USDC-only was this file's earlier choice ("nothing anybody did not choose shows up
# as price drift"); it is the competition's basket now because the rules promise the same
# configuration, and because half of every strategy -- the sell side of an arbitrage, the WETH-
# denominated vault and Trove -- has nothing behind it without base inventory (issue #54). The
# basket's drift is also what the benchmark holds; on returns it is the same drift for everyone who
# kept it.
funding:
  wethWei: "8000000000000000000" # 8 WETH
  base: { WBTC: "40000000" } # 0.4 WBTC (8 decimals)
  usdcUnits: "25000000000" # 25k USDC
  flowWethWei: "150000000000000000000" # 150 WETH per flow wallet (issue #112)
  flowUsdcUnits: "450000000000" # 450,000 USDC per flow wallet
  flowBase: { WBTC: "750000000" } # 7.5 WBTC (#124)

flow:
  uninformedMaxWethWei: "1000000000000000000"
  informedMaxWethWei: "2000000000000000000"
  balancerMaxWethWei: "1000000000000000000"
  curveMaxWethWei: "1000000000000000000"
  informedArbFeeBps: 30
  uninformedArrivalRate: "0.45" # issue #79
  uninformedSizeSigma: "1.5" # issue #79
  uninformedSizeClampMult: "10" # issue #79
  gmxArrivalRate: "0.75"
  gmxSizeSigma: "1.0"
  aaveActorSizeSigma: "1.0"
  baseMax: { WBTC: "5000000" } # 0.05 BTC (8 decimals)
  baseInformedMax: { WBTC: "10000000" } # 0.1 BTC
  # Issue #130: refill every flow wallet's token that fell below half its funded amount, every 300
  # blocks (10 minutes). Funded once, a month of ~12 taker orders a block drains one side until the
  # balance guards turn the flow one-way -- a market that looks like it is trending. The official
  # regimes leave this off; their 360 blocks are calibrated on wallets funded once.
  topUpEveryBlocks: 300

# The LST's economic clock (issue #129). The default counts a block as an hour of staking, which the
# official 360-block regimes were calibrated on: an epoch is 15 days of yield, ~0.12% at 3%/yr. Over
# a 35-day period at 2 s a block that clock is ~170 years; the vault's 50 WETH reward reserve ran dry
# after ~3.3 days, and the redemption rate froze for the rest of the period. 30 s a block keeps the
# official ratio where it matters here -- one scoring day (43,200 blocks) is the same 15 days of
# yield as one official epoch -- and the whole period is ~1.4 years of it, ~4.4 WETH of rewards on
# the seeded 100 WETH: the reserve covers the period with ~1,000 WETH staked. Stake more and it runs
# out early; the observation then reports apyBps 0 and the run records lst_reward_reserve_exhausted.
lst:
  simulatedSecondsPerBlock: 30

# What the period contains. ADR 0021 §1 publishes the *kinds* and how many, and withholds when each
# window opens — the manifest is built from this list and carries neither the windows nor the seed
# they were drawn from.
#
# Every kind of episode the official regimes run, every week, at the official regimes' magnitudes
# and shapes (config/regimes/{crash,spike,cex-drift,informed-flow,whale,depeg,cdp-incident}.yaml).
# `windowFrac` is a fraction of the period, so each block below is one sixth of it (a "week" was six
# weeks' worth; from a 9/27 start to 10/31 it is ~5.8 days); where in its "week"
# each episode lands is the seed's, and the seed is not this file's (see the top).
#
# What the official set has and this cannot: victims (built once, at setup, and gone after the first
# crash that breaks them), a pool that appears mid-epoch (vuln), and a depeg that never comes back
# (`persist` holds to the end of the run, which here is the whole period). `repriceAnchor` is left off for
# the same reason -- in one world it compounds across every drift of the period.
stress:
  events:
    # week 1
    - { type: crash, magnitudeRange: [0.15, 0.22], windowFrac: [0.0050, 0.1617], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.0050, 0.1617], alignWith: crash, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: spike, magnitudeRange: [0.15, 0.22], windowFrac: [0.0050, 0.1617], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.0050, 0.1617], alignWith: spike, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: cexDrift, magnitudeRange: [0.001, 0.002], kappaMultRange: [0.15, 0.3], windowFrac: [0.0050, 0.1617], rampBlocks: 6, holdBlocks: 24, decayBlocks: 12 }
    - { type: flowTrend, magnitudeRange: [2.0, 3.0], trendCorrelation: 1.0, persistBlocks: 12, windowFrac: [0.0050, 0.1617], rampBlocks: 6, holdBlocks: 30, decayBlocks: 10 }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.0050, 0.1617] }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.0050, 0.1617], venue: balancer }
    - { type: depeg, stable: DAI, magnitudeRange: [0.35, 0.6], windowFrac: [0.0050, 0.1617], rampBlocks: 12, holdBlocks: 36, decayBlocks: 45 }
    - { type: eusdDepeg, magnitudeRange: [0.4, 0.6], windowFrac: [0.0050, 0.1617], rampBlocks: 4, holdBlocks: 10, decayBlocks: 12 }
    # week 2
    - { type: crash, magnitudeRange: [0.15, 0.22], windowFrac: [0.1717, 0.3283], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.1717, 0.3283], alignWith: crash, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: spike, magnitudeRange: [0.15, 0.22], windowFrac: [0.1717, 0.3283], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.1717, 0.3283], alignWith: spike, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: cexDrift, magnitudeRange: [0.001, 0.002], kappaMultRange: [0.15, 0.3], windowFrac: [0.1717, 0.3283], rampBlocks: 6, holdBlocks: 24, decayBlocks: 12 }
    - { type: flowTrend, magnitudeRange: [2.0, 3.0], trendCorrelation: 1.0, persistBlocks: 12, windowFrac: [0.1717, 0.3283], rampBlocks: 6, holdBlocks: 30, decayBlocks: 10 }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.1717, 0.3283] }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.1717, 0.3283], venue: curve }
    - { type: depeg, stable: DAI, magnitudeRange: [0.35, 0.6], windowFrac: [0.1717, 0.3283], rampBlocks: 12, holdBlocks: 36, decayBlocks: 45 }
    - { type: eusdDepeg, magnitudeRange: [0.4, 0.6], windowFrac: [0.1717, 0.3283], rampBlocks: 4, holdBlocks: 10, decayBlocks: 12 }
    # week 3
    - { type: crash, magnitudeRange: [0.15, 0.22], windowFrac: [0.3383, 0.4950], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.3383, 0.4950], alignWith: crash, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: spike, magnitudeRange: [0.15, 0.22], windowFrac: [0.3383, 0.4950], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.3383, 0.4950], alignWith: spike, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: cexDrift, magnitudeRange: [0.001, 0.002], kappaMultRange: [0.15, 0.3], windowFrac: [0.3383, 0.4950], rampBlocks: 6, holdBlocks: 24, decayBlocks: 12 }
    - { type: flowTrend, magnitudeRange: [2.0, 3.0], trendCorrelation: 1.0, persistBlocks: 12, windowFrac: [0.3383, 0.4950], rampBlocks: 6, holdBlocks: 30, decayBlocks: 10 }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.3383, 0.4950] }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.3383, 0.4950], venue: balancer }
    - { type: depeg, stable: DAI, magnitudeRange: [0.35, 0.6], windowFrac: [0.3383, 0.4950], rampBlocks: 12, holdBlocks: 36, decayBlocks: 45 }
    - { type: eusdDepeg, magnitudeRange: [0.4, 0.6], windowFrac: [0.3383, 0.4950], rampBlocks: 4, holdBlocks: 10, decayBlocks: 12 }
    # week 4
    - { type: crash, magnitudeRange: [0.15, 0.22], windowFrac: [0.5050, 0.6617], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.5050, 0.6617], alignWith: crash, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: spike, magnitudeRange: [0.15, 0.22], windowFrac: [0.5050, 0.6617], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.5050, 0.6617], alignWith: spike, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: cexDrift, magnitudeRange: [0.001, 0.002], kappaMultRange: [0.15, 0.3], windowFrac: [0.5050, 0.6617], rampBlocks: 6, holdBlocks: 24, decayBlocks: 12 }
    - { type: flowTrend, magnitudeRange: [2.0, 3.0], trendCorrelation: 1.0, persistBlocks: 12, windowFrac: [0.5050, 0.6617], rampBlocks: 6, holdBlocks: 30, decayBlocks: 10 }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.5050, 0.6617] }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.5050, 0.6617], venue: curve }
    - { type: depeg, stable: DAI, magnitudeRange: [0.35, 0.6], windowFrac: [0.5050, 0.6617], rampBlocks: 12, holdBlocks: 36, decayBlocks: 45 }
    - { type: eusdDepeg, magnitudeRange: [0.4, 0.6], windowFrac: [0.5050, 0.6617], rampBlocks: 4, holdBlocks: 10, decayBlocks: 12 }
    # week 5
    - { type: crash, magnitudeRange: [0.15, 0.22], windowFrac: [0.6717, 0.8283], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.6717, 0.8283], alignWith: crash, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: spike, magnitudeRange: [0.15, 0.22], windowFrac: [0.6717, 0.8283], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.6717, 0.8283], alignWith: spike, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: cexDrift, magnitudeRange: [0.001, 0.002], kappaMultRange: [0.15, 0.3], windowFrac: [0.6717, 0.8283], rampBlocks: 6, holdBlocks: 24, decayBlocks: 12 }
    - { type: flowTrend, magnitudeRange: [2.0, 3.0], trendCorrelation: 1.0, persistBlocks: 12, windowFrac: [0.6717, 0.8283], rampBlocks: 6, holdBlocks: 30, decayBlocks: 10 }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.6717, 0.8283] }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.6717, 0.8283], venue: balancer }
    - { type: depeg, stable: DAI, magnitudeRange: [0.35, 0.6], windowFrac: [0.6717, 0.8283], rampBlocks: 12, holdBlocks: 36, decayBlocks: 45 }
    - { type: eusdDepeg, magnitudeRange: [0.4, 0.6], windowFrac: [0.6717, 0.8283], rampBlocks: 4, holdBlocks: 10, decayBlocks: 12 }
    # week 6
    - { type: crash, magnitudeRange: [0.15, 0.22], windowFrac: [0.8383, 0.9950], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.8383, 0.9950], alignWith: crash, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: spike, magnitudeRange: [0.15, 0.22], windowFrac: [0.8383, 0.9950], rampBlocks: 3, holdBlocks: 6, decayBlocks: 8 }
    - { type: liquidityPull, magnitudeRange: [0.4, 0.6], windowFrac: [0.8383, 0.9950], alignWith: spike, rampBlocks: 3, holdBlocks: 6, decayBlocks: 12 }
    - { type: cexDrift, magnitudeRange: [0.001, 0.002], kappaMultRange: [0.15, 0.3], windowFrac: [0.8383, 0.9950], rampBlocks: 6, holdBlocks: 24, decayBlocks: 12 }
    - { type: flowTrend, magnitudeRange: [2.0, 3.0], trendCorrelation: 1.0, persistBlocks: 12, windowFrac: [0.8383, 0.9950], rampBlocks: 6, holdBlocks: 30, decayBlocks: 10 }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.8383, 0.9950] }
    - { type: whale, magnitudeRange: [25, 60], windowFrac: [0.8383, 0.9950], venue: curve }
    - { type: depeg, stable: DAI, magnitudeRange: [0.35, 0.6], windowFrac: [0.8383, 0.9950], rampBlocks: 12, holdBlocks: 36, decayBlocks: 45 }
    - { type: eusdDepeg, magnitudeRange: [0.4, 0.6], windowFrac: [0.8383, 0.9950], rampBlocks: 4, holdBlocks: 10, decayBlocks: 12 }

# The roster is a registration list, not a launch list (ADR 0021 §2).
#
#   external: true + address   the participant holds their own key. Prefer this: a key the operator
#                              generated is a key the operator has.
#   external: true + wallet    the operator issues a funded key and hands it over.
#
# `noop` is the baseline every score is measured as excess over (ADR 0019 §2) and is run by the
# operator, so it stays an ordinary entry.
# `wallet: AUTO` rather than a named dev key. On a chain the environment cannot assign balances, the
# endowment is a floor: an address that already holds something keeps it. Two agents bound to
# prefunded dev accounts once started a run with $3.0bn against a fresh address's $34k, and their
# scores were a report on one large ETH holding. The run records `initial_endowment` and warns above
# a 2x spread, but a level field starts with fresh addresses.
# `participant` is the rules §2.2 unit (a person or a team). A unit may enter two agents and is
# scored on the higher; two entries with the same `participant` are that unit's two submissions.
# Optional -- an entry without one is its own unit.
# The resident field. A practice period whose standings are one flat line teaches nobody anything:
# a participant needs to see that the number moves, in both directions, and to have something to be
# above or below. These are the operator's own reference strategies, running continuously for the
# whole period, and they are what a newly registered agent lands among.
#
# Every one that ships a prompt.md is FROZEN. Two reasons, and the second is the expensive one:
#   - a benchmark that rewrites itself is not a benchmark; the line a participant compares against
#     has to be the same strategy on day 39 as on day 1
#   - the improve loop calls a model every `reviseEveryBlocks`. Over a 39-day period at 2 s blocks
#     that is a continuous inference bill for agents nobody is scoring.
#
# Venue coverage follows run.protocols above. The liquidator is still not here: it acts on the
# victims a regime seeds, and this period seeds none (see stress above), so it would idle.
agents:
  - id: noop
    wallet: AUTO
    baseline: true
    description: does nothing — the benchmark every score is excess over
  - id: random
    wallet: AUTO
    description: trades at random, deterministically seeded — the lower control, so "below the
      baseline" is visibly reachable and not just a rounding artefact
  - id: venue-arb
    wallet: AUTO
    env: { ERIS_AGENT_FROZEN: "1" }
    description: WETH-only cross-venue arbitrage — the simplest thing that works, and the line most
      first submissions are really competing with
  - id: clean-arb
    wallet: AUTO
    description: two-leg round trip priced against fees, impact and expected adverse move (no
      prompt.md — a pure rule strategy, nothing to freeze)
  - id: multi-arb
    wallet: AUTO
    env: { ERIS_AGENT_FROZEN: "1" }
    description: base-agnostic cross-venue arbitrage (all active bases × all venues)
  - id: peg-arb
    wallet: AUTO
    env: { ERIS_AGENT_FROZEN: "1" }
    description: stablecoin peg arbitrage — the EUSD/DAI side, which the liquity venue supplies
  - id: lst-carry
    wallet: AUTO
    env: { ERIS_AGENT_FROZEN: "1" }
    description: the LST vault's carry, so the lst venue has someone reading it
  - id: basis-arb
    wallet: AUTO
    env: { ERIS_AGENT_FROZEN: "1" }
    description: spot against the GMX perp, so the gmx venue has someone reading it
  - id: levered-long
    wallet: AUTO
    env: { ERIS_LEVER_TARGET_HF: "1.8" }
    description: borrows on Aave against its collateral to hold more WETH than it was funded with

  # A participant registers here, or (without a restart) in config/registrations.yaml:
  # - id: alice
  #   external: true
  #   address: "0x..."
  #   participant: team-alice
  #   description: a registered participant
