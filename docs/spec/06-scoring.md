[← 目次](README.md) ｜ [← 05 エージェント契約](05-agent-contract.md) ｜ [07 設定 →](07-configuration.md)

# 06. 採点

出典 `core/src/scoring/{epochScore,metrics,aggregate}.ts`、`core/src/realtime/{liveScoring,reconstruct}.ts`、`core/src/backtest/standings.ts`、`sdk/src/{valuation,pnl,stables}.ts`。

## 6.1 採点の 4 層

```
[1] 保有 → USDC        トークン種別と venue アダプタが値付ける
[2] 横断面 → 系列       エポック境界で全エージェントを同一ブロックで読む
[3] 系列 → スコア       M9 = mean(x_e) − λ·std(x_e)
[4] シナリオ → 順位     シナリオ内 z-score → レジーム等重み平均
```

**層 1〜3 は 1 つの run で完結し、層 4 はシナリオ行列でのみ存在する。** 各層の出力はすべて保存されるので、下層を再実行せずに上層だけ差し替えられる（[00 §0.5 P5](00-overview.md)）。

## 6.2 層 1：保有の値付け

### spot（`sdk/src/valuation.ts`）

`tokenAmountUsd(token, amount, fairByBase, stablePrices)` が単一の入口。

| トークン | 価格 |
|---|---|
| `kind: "base"` | その run の fair price |
| `kind: "stable"` | 市場が払う額（`stablePriceUsdc`）。USDC は $1 固定 |
| レジストリ外の USDC 変種（USDC.e / USD₮0） | 6 decimals・$1 の慣行 |
| その他 | **`undefined` = 値付け不能**。0 ではない |

「値付けできない」を 0 にせず `undefined` で返すのが要点。以前は固定の位置型を列挙して他をすべて 0 としていたため、**リストに無い venue に価値を移すと全損として読めた**。

LP トークンは**プールの準備金に対する比例持分**で値付ける（`poolShareValueUsdc`）。Balancer weighted も Curve crypto も手数料なしで比率どおりに exit できるので、比例持分が実現可能な exit 価値そのものになる。venue 固有の価格式（virtual price / BPT rate）に依存しないという利点もある。

### venue（アダプタの `valueAtBlock`）

[01 §1.4](01-architecture.md) の段階生成器。アダプタは `valueUsdc`（マーク）と `liquidatableValueUsdc`（実現可能額）を返す。**採点が合計するのは前者**。

### 値付けから外れた保有の報告

`ScoringExclusionReason` は 4 種（`valuation.ts:42`）。

| reason | 意味 | 値に含まれるか |
|---|---|---|
| `unpriced` | 数量は分かるが USD 価格が無い | 含まない |
| `read-failed` | 読み取りに失敗したので保有が**不明**（0 ではない） | 含まない |
| `unrealizable` | 値付けはできるが run 終了までに換金できない（LST のキュー） | 含まない |
| `par-fallback` | 市場が quote を返さなかったので **$1 として計上した** | **含む** |

4 種すべてを `summary.json` の `valueSeries.unpricedHoldings` に報告する。**`summary.json` の 0 が取引損失と誤読されてはならず、1 ドルもまた同じ。**

### 未申告トークンの検出

`findUnaccountedTokens` が Transfer ログから「誰も合計していない ERC-20」を洗い出す。アダプタは `accountedTokens()` で「自分が値付けている / 集計値に含まれている」トークンを申告する。**venue が発行するが値付けていないトークンは申告しない**（見えたままにするため）。実例：Stability Pool の LQTY gain は `erc20-unaccounted` として 61.3 LQTY と報告された。

## 6.3 層 2：横断面と系列

### 2 つの経路

| 経路 | いつ | 何を作るか |
|---|---|---|
| **live**（`LiveScorer`） | エポック境界を通過するたび | 採点に使うエポック系列。`epochs.jsonl` と `epoch_boundary` イベント |
| **sweep**（`reconstructValueSeries`） | run 終了後（窓が 1,000 ブロック以内のとき） | equity curve・α・`unpricedHoldings`・`market.json` |

**両者は同じ reader（`readValueSnapshotAtBlock`）・同じブロック・同じ G7 median 窓を使う**ので一致する。それが「live が sweep の代替になる」という主張の根拠であり、両方存在する run では毎回 `epoch_series_agreement` で検査する（[11](11-invariants.md)）。

**live が必要な理由**（`liveScoring.ts:1-18`）：

1. 止まらないチェーンには「あと」が来ない
2. ノードの履歴保持深度は有限（anvil は約 1,050 ブロック）。「run を短くする」では 1 週間のチェーンに対処できない

### エポック境界

`epochBoundaryBlocks(fromBlock, toBlock, epochBlocks)`。E エポックには E+1 個の境界が要り、run の開始が境界 0 になる。

**末尾の端数エポックは落とす。** 他より短い窓は構造的に小さい log return を生み、指標はそれを「エージェントが減速した」と読んでしまう。

`--score-every N` は equity curve の間引きで、`fromBlock` と `toBlock` は必ず含む。**スコアは不変**（α は最初と最後の横断面しか使わない）。

### 読めなかった境界

**境界を記録しない**（`null` で埋めない）。系列は「値が無い」と「0」を区別する。境界ブロック自体を push しないので、系列は実際に読めた境界と整合したままになる。

### G7：マーク median（`MarkMedian`）

エポック境界を、**その直前 `markMedianBlocks` ブロックの median で評価する**（既定 5）。1 ブロックだけプールを押した結果がスコアになるのを防ぐ。窓の大半で成立していなければ効かないので、スプレッドコストを払う往復が「ポジション」に変わる。

**対象は市場価格 stable だけ**で、それで全面をカバーしている。

| 対象 | median するか | 理由 |
|---|---|---|
| 市場価格 stable（spot・Trove 債務・SP 預入） | **する** | プールの quote が**そのままマーク**であり、取得原価は別の場所にある。プールを動かすとスコアが動く |
| LP 持分 | しない | 準備金 × 環境の fair price で評価する。プールを押すとエージェントの 2 つのバケツの間で価値が移るだけ |
| LST | しない | 採点マークは額面（償還レート × WETH fair）でプールを一切読まない |

live 側と sweep 側で同じ窓を使う。実際に median がどれだけ効いたかは `valueSeries.markMedian.maxDeviationBps` に出る。

### α（β 除去 PnL）

sweep のみが作る。**固定した参照 fair price**（`alphaRefFairUsdcPerWeth`）で free inventory を評価し、`alphaByAgent = alphaLast − alphaFirst` を取る。最初と最後の横断面しか使わないので、間引きの影響を受けない。

**α が除去するのは free inventory の β だけ**である。venue ポジション（LST など）は live mark なので、USDC 建て採点では LST 保有戦略は構造的に β で不利になる（実測：noop 0 > lst-carry −203 > lst-carry-wide −233、一方 WETH を持たない venue-arb は +115）。

## 6.4 層 3：スコア（M9）

出典 `core/src/scoring/epochScore.ts:71`。

```
x_e   = ln(W_e / W_{e−1}) − ln(B_e / B_{e−1})      e = 1..E
W     = max(V, 0.01 × W_init)                      G1：破産フロア
score = mean_e(x_e) − λ · std_e(x_e)               λ = 0.25（既定）
```

- `std` は**母標準偏差**（分母 E、E−1 ではない）。系列は「週そのもの」であって、そこから引いたサンプルではない
- `E`（分母）は**フィールド全体で固定**する。「そのエージェントが生き延びたエポック数」で割ると、短い系列は std が小さくなり**早く失敗することが得になる**（ADR 0019 §8）
- 初期値が無い / 正でないエージェントはスコアを返さない（`null`）。par や「最初に値を報告した時点」を代入するのは、誰も測っていないエージェントに数字を付けることになる

### ベンチマーク（超過収益）

`B` はロスターの `baseline: true` エントリの系列。**これは化粧ではない。**

現金を持っているだけのエージェントは mean も std もきっかり 0 でなければ、λ は「何もしない」ことに対するハードルにならない。実測（B harness 5 seed）：生収益で採点すると noop が −5.07e-5・std 2.22e-4 で、これは**アクティブなエージェントの全分散の 93%** に相当した — 全員が持つガス準備が ETH と一緒に動くためである。全員が同じ準備を持つのでエポックごとに相殺され、差し引くと peg-arb の Sharpe は 0.111 → 0.241 に、noop はきっかり 0 になった。

mean 項はベンチマークの選択に影響されない（log return は telescope するので定数になる）。**変わるのは std 項だけ**であり、それがベンチマークを HODL ではなく現金にした理由（ADR 0019 §2）。

ベンチマーク自身も自分に対して採点され、行が 0 として存在する（レポートから消えない）。ロスターにベンチマークが無ければ警告を出して生収益にフォールバックする。

### G1 / G2：破産

| ガード | 内容 |
|---|---|
| **G1** | 価値を初期資本の 1%（`floorFraction`）で下から抑える |
| **G2** | フロアに触れたエポック以降、系列を **0（超過ゼロ）で凍結**する |

G1 は `ln(0)` 回避だけではない。**Aave の `valueUsdc` は担保 − 債務で clamp されていない**ので、清算されたエージェントの総価値は負になりうる（`ln` は −∞ ではなく NaN）。フロアがあることで破産が定義済みのイベントになり、G2 がそこにぶら下がれる。同時に単一エポックの最悪リターンが有界になり、1 件の吹き飛びがフィールド全員の std を支配しなくなる。

**G2 は採点規則であってチェーン規則ではない。** エージェントの tx はブロックされない — 実チェーンでは参加者がシーケンサに直接届くので、止められるのはシーケンサ級の権力だけである（ADR 0019 §5）。

### 欠測の扱い

読めなかった境界は**直前値を持ち越して return 0** とし、`carriedForwardEpochs` に記録する。エポックを落とすと、**環境が読めなかったエージェントだけ系列が短くなり std が小さくなる**。ギャップは環境の失敗であってエージェントの失敗ではない。

## 6.5 候補指標（`npm run metrics`）

`core/src/scoring/metrics.ts`。**同じエポック系列**から全候補を計算する。指標を差し替えて保存済み run を採点し直せるという ADR 0017 §4 / ADR 0019 の約束を実行するモジュール。

| 番号 | フィールド | 定義 |
|---|---|---|
| M1 | `totalPnlUsdc` | 端点差（USDC）。リスク中立 |
| M4 | `excessLogGrowth` | ベンチマーク超過の対数成長。**両脚を自分の初期値で正規化する**ので、配布額が違っても比較が歪まない |
| M7 | `mppm` | MPPM（Goetzmann et al. 2007）。ρ=1 は式が定義されないので平均対数収益を返す |
| M9 | `score` | `mean − λ·std`（現行の採点） |
| M13 | `sharpePerEpoch` / `sharpeOverRun` | エポック Sharpe と `× √E`。**見出し指標の候補ではない**（比は尺度不変なので「小さく安全に」が最適化される）が、λ はこれに対する閾値なので表に載る |
| M27 | `bordaTotals` | run 集合に対する Borda。順序尺度なので、分散が 10 倍のレジームが支配できない |

**M4 と M9 の関係**：log return は telescope するので `sum(x_e) = E·mean(x_e)`。よって両者は**きっかり `λ·std` だけ違う**。**順位が動くのは、そのエージェントの 1 エポックあたり Sharpe が λ を跨いだときだけ**である。

`npm run metrics` は `--lambda` / `--rho` / `--out` を取り、**`resetUnit` が混ざった run 集合を拒否する**（[02 §2.4](02-runtime.md)）。

## 6.6 層 4：シナリオ行列の順位

### 現行ルール（`core/src/backtest/standings.ts`）

```
1. シナリオごとのスコア      その run の生の指標値
2. シナリオ内 z-score        そのシナリオを走った全エージェントで正規化（母標準偏差）
3. 合計                      レジーム内平均 → レジーム等重み平均
```

**なぜレジーム単位ではなくシナリオ単位で正規化するのか**：1 つのシナリオの全エージェントは同じ world を走った（ADR 0017 §1 の co-location）。設計が公平性を保証している比較はそれだけである。副次的に、レジーム間だけでなく seed 間のスケール差も潰れる。

**レジーム等重み**なので、seed の多いレジームが合計に多く効くことはない（ADR 0017 §3）。

### 失格（disqualified）

| 事象 | 扱い |
|---|---|
| プロセスが途中で死んだ / ルール違反 / 一度も報告しなかった | 失格。**最下位の finisher よりさらに 1 標準偏差下**（`DISQUALIFIED_Z_PENALTY = 1`） |
| summary が読めるのに当該指標が無い | 同じく失格（報告の失敗を「平均的な結果」と見なさない） |
| そもそも summary.json が無い | **シナリオごと除外**して別途報告する。環境の失敗を参加者に負わせない |

失格は**最下位より悪く**なければクラッシュが戦略になる。同時に **−∞ ではなく有界**でなければ 1 本の悪いシナリオが競技全体を決めてしまう。

### 選べる指標（`ScoringMetric`）

`netPnlUsdc`（既定）/ `alphaUsdc` / `excessLogGrowth` / `score`。既定が `netPnlUsdc` なのは**過去の行列と比較できる唯一の指標**だから。

**M4 と M9 はエポック系列から採る**（`summary.json` の `epochScores`）。M4 を端点からではなく「M9 が採点したのと同じ系列の合計」から取ることで、破産したエージェントの M4 が G1/G2 で凍結した後の系列を反映する。

### 集約方式（`core/src/scoring/aggregate.ts`）

`scenario` モードには**「どの指標か」に加えて「シナリオ横断でどう集約するか」という第 2 の選択**がある。

| aggregator | 内容 | 弱点 |
|---|---|---|
| `zscore` | 現行。シナリオ内で尺度自由 | **issue #55**：1 体の極端な結果が場の sd を膨らませ、他全員を圧縮する |
| `borda` | シナリオ内順位を点数化して平均。極端な結果でも 1 順位しか失わない | 「どれだけ勝ったか」を捨てる（1 USDC 差と圧勝が同じ） |
| `mean` | 指標そのものを平均。絶対スケールを保つ | 分散の大きいレジーム 1 本が合計を支配しうる |

3 つとも外側の重み付け（レジーム内平均 → レジーム等重み）は共通で、すべて「高いほど良い」向きに揃えてある。

**`sdInflationFromExtreme`** が issue #55 を数値にする：最も極端な 1 体を除いたときの sd に対する比。**1.0 なら誰も場のスケールを決めていない**。実測では 1 体の −1,113 USDC が場の sd を 20.9 → 181.5 に膨らませ（比 8.7）、z-score はその sd で割るので他全員のスコアが同じ倍率で圧縮された。

`npm run metrics -- --matrix runs/matrix-<id>` が「指標 × 集約」の総当たりで採点し直し、各組み合わせの順位・M9×zscore との一致/不一致・#55 の露出を出す。

## 6.7 順位表の表示規則（ダッシュボード）

参加者向けにルールを固定してある（2026-08-31）。

- 順位はシナリオごと **M9（λ=0.25）→ シナリオ内 z-score → レジーム等重み平均**
- **スコア列に表示するのは M9 のレジーム等重み平均そのもの**（×10⁴ スケール・単位表記なし）。z 集約値はツールチップに降格する（無単位の z は「どれだけ差があるか」を答えられないため）
- 参考列として net PnL（final marks）の合計を 1 列だけ併記する（β が相殺され `noop` がきっかり 0 になる方の量）
- **表示値と順位は稀に前後し得る。** それは集約方式の差そのものなのでキャプションに明記する
- 集約は `core/src/scoring/aggregate.ts` を**ダッシュボードが直接 import する**（2 箇所に置くと CLI と画面で順位が食い違ったとき、どちらが本物か分からなくなる）

詳細は [09](09-dashboard.md)。

## 6.8 採点に関する未決事項

| 論点 | 状態 |
|---|---|
| **λ の `scenario` 側較正** | 既知値（ADR 0019 の 0.25、測定記録の推奨 0.15）はどちらも**連続経済 × 12 ブロックエポック**のもの。1 シナリオのエポック数はシナリオ数 S に依存し、S は未決 |
| **指標の最終決定（M4 vs M9）** | 「より稼ぐが荒いエージェント」と「安定したエージェント」のどちらを上位にするかは再計算では決まらない。ADR 0019 はリスク調整指標を選んだが、issue #56 は開いている |
| **集約方式の決定** | ADR 0019 は現行の z-score を「後継を挙げずに退役」と宣言した |
| **LST の採点基礎** | 現行実装は par（額面）。issue #38 の意図は realizable。`lst` が競技セットに入る前に決める |

→ [12 既知の制約・未決事項](12-open-issues.md)
