{
  "name": "@eris/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Contract layer for eris-competition (ADR 0015). Types, action schema, chain IO, and protocol adapters that both the environment (core) and participants (example) depend on.",
  "exports": {
    ".": "./src/index.ts",
    "./*.js": "./src/*.ts",
    "./*": "./src/*"
  },
  "dependencies": {
    "viem": "^2.39.3",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
