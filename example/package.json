{
  "name": "eris-example-agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Participant template (ADR 0015). agents/ is the unit of copy/submission. Depends only on sdk (importing core is forbidden).",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.98.0",
    "@eris/sdk": "*",
    "viem": "^2.39.3",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  }
}
